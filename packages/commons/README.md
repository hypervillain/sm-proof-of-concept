# Utilities used across repositories

* validate package name
* validate folder structure
* validate models, previews, slice names..
* build slices definition
* validate parse metadata (`sm.config.json`)